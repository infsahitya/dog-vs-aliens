import { Enemy } from ".";
import { ENEMY_PROPS } from "./__enemy";

class __zombieBat extends Enemy implements ENEMY_PROPS {
  update(deltaTime: number) {}
  draw() {}
}

export default __zombieBat;
